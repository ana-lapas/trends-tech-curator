version: '3.8'
services:
  curator:
    build: .
    volumes:
      # This maps the database file from the container to your local folder
      - ./curator_data.db:/app/curator_data.db
    env_file:
      - .env
    # Optional: Run as a cron job if you want it to run periodically
    restart: on-failure